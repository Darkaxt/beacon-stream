# Extraction Map

| Source | Planned Use | Copy Source Now | Boundary |
| --- | --- | --- | --- |
| Sunshine | Streaming protocol, capture, encode, audio, input reference; Milestone 10/17/18/40 external process boundary, Beacon-owned connection metadata, wrapper readiness manifest, Milestone 41 input contract, Milestone 43 Windows input targeting principle, Milestone 44 input health reporting, Milestone 47 keyboard action boundary, Milestone 52 runtime session descriptor handoff, Milestone 53 no-phone wrapper probe, Milestone 54 wrapper API smoke coverage, Milestone 56 fake-endpoint wrapper handoff coverage, Milestone 57 wrapper process startup hardening, Milestone 58 documented port-profile metadata, Milestone 59 no-phone profile-wrapper integration coverage, Milestone 60 static endpoint health visibility, Milestone 62 child-process wrapper harness, Milestone 63 server-owned child wrapper configuration, Milestone 64 child wrapper smoke coverage, Milestone 65 structured child-wrapper health visibility, Milestone 66 child-wrapper config invariant hardening, Milestone 90 Android RTSP message/session boundary, Milestone 91 RTSP handshake sequencing, Milestone 92 RTSP SETUP negotiation, Milestone 93 RTSP ANNOUNCE/PLAY sequencing, Milestone 94 RTSP byte-stream transport, Milestone 95 RTSP socket lifecycle, Milestone 96 APK RTSP route wiring, Milestone 97 RTSP-to-video handoff, Milestone 98 RTP packet/sample boundary, Milestone 99 client RTP port retention, Milestone 100 UDP RTP datagram source, Milestone 105 RTP sequence reordering, and Milestone 106 H.264 parameter-set metadata reference only | No | `src/Beacon.Platform.Windows/Streaming/ExternalProcessStreamingBackend.cs` passes Beacon plans, optional connection descriptors, a Beacon-owned manifest path, a derived Sunshine endpoint profile, a configured wrapper child executable/arguments pair, and a fresh runtime session descriptor path to a configured wrapper without copying Sunshine source; it also reports advertised static endpoints, structured child executable/argument readiness, rejects orphan child arguments, and preflights missing configured child binaries in Beacon-owned health diagnostics; `src/Beacon.Platform.Windows/Streaming/SunshineEndpointProfile.cs` encodes documented public port offsets from LizardByte Sunshine docs without copying source; `src/Beacon.Platform.Windows/Streaming/WindowsExternalStreamingProcessRunner.cs` starts the configured wrapper from its executable directory without copying Sunshine source; `src/Beacon.Platform.Windows/Streaming/WindowsExternalStreamingSessionDescriptorStore.cs` reads Beacon-owned runtime descriptor JSON written by a wrapper; `src/Beacon.StreamingProbe` is an original no-phone wrapper probe that writes the same descriptor contract and can supervise a configured child executable before publishing runtime descriptor evidence without implementing Sunshine protocol; `src/Beacon.FakeEndpoint` is an original scripted control-plane simulator that can assert the Beacon-owned stream descriptor; `tests/Beacon.Server.Tests/ClientApiTests.cs` launches that original probe through the real Windows external-process runner while keeping display and game side effects fake, including Sunshine profile-derived and child-process handoff coverage; `src/Beacon.Core/Input/ClientInput.cs` defines an original input forwarding and input-health contract without copying Sunshine source; `src/Beacon.Platform.Windows/Input/WindowsClientInputSink.cs` resolves Beacon display topology before dispatching pointer or keyboard input and reports its supported surface without copying Sunshine source; `src/Beacon.Android/app/src/main/java/dev/beacon/android/RtspRequest.java`, `RtspResponse.java`, `GameStreamEndpointPlan.java`, `GameStreamRtspSessionClient.java`, `GameStreamRtspSessionInfo.java`, `GameStreamVideoSessionClient.java`, `RtpPacket.java`, `RtpPacketSource.java`, `RtpReorderingPacketSource.java`, `H264ParameterSets.java`, `GameStreamRtpVideoSampleProvider.java`, `GameStreamRtpPacketSourceFactory.java`, `GameStreamRtpVideoConsumer.java`, `GameStreamRtpVideoSessionClient.java`, `RtpDatagramSocket.java`, `RtpDatagramSocketFactory.java`, `RtpDatagramPacketSource.java`, `JavaRtpDatagramSocketFactory.java`, `GameStreamUdpRtpPacketSourceFactory.java`, `RtspTransport.java`, `GameStreamRtspHandshakeClient.java`, `GameStreamRtspSetupResult.java`, `GameStreamRtspSdpPayloadProvider.java`, `RtspByteStreamTransport.java`, `RtspTransportException.java`, `RtspTransportLease.java`, `RtspTransportLeaseFactory.java`, `GameStreamRtspTransportSessionClient.java`, `RtspSocketConnector.java`, `RtspSocketHandle.java`, `JavaRtspSocketConnector.java`, `RtspSocketTransportLeaseFactory.java`, `AndroidNativeStreamClientFactory.java`, and `BeaconViewModelSession.java` are original Java boundary code for future GameStream session/media work and do not copy Sunshine source; `docs/external-streaming-wrapper-manifest.md` and `docs/source-audits/2026-07-08-windows-input-sink-upstream-audit.md` document adapter/input boundaries without copying Sunshine source |
| Moonlight/Artemis Android clients | Android client behavior reference for local-vs-server ownership, client-local input settings, touch/input UX, launch delegation, decoder/audio composition, and GameStream session expectations | No | `src/Beacon.Android` retains original Beacon control-plane and diagnostic Java code; Milestone 110 uses Moonlight/Artemis JNI, decoder, audio, and connection composition only as reference, and Milestone 112 follows their mature native-connection/decoder composition boundary without importing product UI or settings code. The boundary is recorded in `docs/source-audits/2026-07-10-moonlight-native-core.md`. |
| Moonlight common C | Android GameStream transport, crypto, media packet, and native input core | Yes, pinned recursive submodule | Milestone 110 adds `src/Beacon.Android/streaming-moonlight/src/main/cpp/moonlight-common-c` at `2ea47752c3051d72a64bcca190024e8b354fa1ef`; Milestone 111 adds an original Beacon server wire contract and Java mapping for the public `SERVER_INFORMATION`, `STREAM_CONFIGURATION`, and documented constant values; Milestone 112 adds Beacon-owned JNI that calls `LiStartConnection`, forwards connection/video callbacks, and feeds native decode units into Beacon's bounded Surface-backed `MediaCodec` path. Pairing and host `/launch` provisioning remain separate server work. |
| Apollo | SudoVDA integration, display lifecycle lessons, dynamic app discovery reference, and display-preparation ordering reference | No | Milestone 2/3 only after focused source audit; Milestone 43 source audit records display/session ordering comparison without copied source |
| Apollo `third-party/sudovda/sudovda-ioctl.h` | SudoVDA interface GUID, protocol version, and IOCTL contract constants for C# driver boundary | Adapted protocol facts | `src/Beacon.Platform.Windows/Displays/WindowsDisplayApi.cs` |
| Local Steam files | Installed apps, library folders, and non-Steam shortcut launch ids | Parsed local user data only | `src/Beacon.Core/Games/Steam` and `src/Beacon.GameProbe` |
| Local Heroic files | Installed GOG and sideloaded app metadata | Parsed local user data only | `src/Beacon.Core/Games/Heroic` |
| Local Hydra database | Installed game rows and executable hints | Read-only SQLite query | `src/Beacon.Core/Games/Hydra` |
| SteamGridDB | Cover artwork lookup when an API key is supplied | API responses only, no key committed | `src/Beacon.Core/Games/SteamGridDb` |
| Windows user32/ntdll/process APIs | Session activity inspection for launched processes, child processes, and top-level windows | Native API calls only | `src/Beacon.Platform.Windows/Sessions` |
| Vibeshine | HDR/driver research reference and virtual-display lease/readiness comparison | No | Research notes only until a specific patch is chosen; Milestone 43 source audit records lease/readiness comparison without copied source |
| Vibepollo | Settings complexity anti-patterns and selected research reference | No | Research notes only |
| ApolloDisplayRescue | WPF recovery behavior reference | No | Milestone 4 only after focused source audit |

Milestone 101 note: `GameStreamRtpDecoderVideoConsumer.java`, the GameStream RTP metadata conversion in `EncodedVideoStreamPlan.java`, and the `GameStreamRtpVideoConsumer.stop()` lifecycle are original Android boundary code. They connect Beacon's already-fakeable RTP sample provider to Beacon's existing `SurfaceEncodedVideoDecoder` abstraction when server-owned metadata advertises codec/container/width/height/fps. They do not copy Sunshine, Moonlight, Artemis, Apollo, or Vibepollo source, and live GameStream RTP depacketization remains a later boundary.

Milestone 102 note: the metadata gate in `GameStreamNativeStreamClient.java`, the three-argument route composer in `AndroidNativeStreamClientFactory.java`, and the Activity wiring in `BeaconActivity.java` are original Android composition code. They keep descriptors without RTP decoder metadata on the RTSP-only path and route metadata-bearing descriptors into Beacon's UDP RTP sample-provider plus decoder-consumer boundary. They do not copy Sunshine, Moonlight, Artemis, Apollo, or Vibepollo source, and dynamic port leasing plus GameStream-specific depacketization remain later work.

Milestone 103 note: `H264RtpSampleProvider.java` and the codec-based sample-provider selection in `GameStreamRtpVideoSessionClient.java` are original Java code implementing Beacon's H.264 RTP payload boundary for single NAL, STAP-A, and FU-A packets into Annex B samples. They are based on the public RFC 6184 packetization contract, do not copy Sunshine, Moonlight, Artemis, Apollo, Vibeshine, or Vibepollo source, and leave HEVC, AV1, jitter timing, dynamic client port leasing, sequence reordering, and descriptor-metadata parameter-set injection for later milestones.

Milestone 104 note: `GameStreamRtpPortLease.java`, `GameStreamRtpPortLeaseFactory.java`, the RTP-lease wiring in `GameStreamRtspTransportSessionClient.java` and `GameStreamRtspHandshakeClient.java`, the lease attachment in `GameStreamRtspSessionInfo.java`, and the leased-video-socket path in `GameStreamUdpRtpPacketSourceFactory.java` are original Java code for Beacon's Android RTP port ownership boundary. They dynamically bind local UDP sockets, advertise those actual ports during RTSP SETUP, and transfer the leased video socket into RTP packet intake without copying Sunshine, Moonlight, Artemis, Apollo, Vibeshine, or Vibepollo source.

Milestone 105 note: `RtpReorderingPacketSource.java` and the wrapper wiring in `GameStreamRtpVideoSessionClient.java` are original Java code for Beacon's bounded RTP sequence-ordering boundary. The implementation uses RTP sequence-number arithmetic to buffer small out-of-order arrivals, drop late/duplicate packets, recover from missing packets by packet count, and flush buffered packets at end-of-source without copying Sunshine, Moonlight, Artemis, Apollo, Vibeshine, or Vibepollo source.

Milestone 106 note: `H264ParameterSets.java`, the parameter-set injection path in `H264RtpSampleProvider.java`, and the metadata alias wiring in `GameStreamRtpVideoSessionClient.java` are original Java code for Beacon's H.264 descriptor-metadata SPS/PPS boundary. The implementation accepts `sprop-parameter-sets` shaped Base64 NAL metadata from Beacon descriptors and prepends decoded Annex B parameter sets before the first VCL sample without copying Sunshine, Moonlight, Artemis, Apollo, Vibeshine, or Vibepollo source.

Milestone 107 note: `GameStreamRtspSdpMetadata.java`, RTSP response body retention in `RtspResponse.java` and `RtspByteStreamTransport.java`, the H.264 SDP metadata field in `GameStreamRtspSessionInfo.java`, and the fallback wiring in `GameStreamRtpVideoSessionClient.java` are original Java code for Beacon's RTSP DESCRIBE SDP SPS/PPS boundary. The implementation extracts H.264 `sprop-parameter-sets` from the SDP video section and uses it as a fallback for H.264 RTP parameter-set injection without copying Sunshine, Moonlight, Artemis, Apollo, Vibeshine, or Vibepollo source.

Milestone 108 note: the access-unit assembly state in `H264RtpSampleProvider.java` and session coverage in `GameStreamRtpVideoSessionClientTest.java` are original Java code for Beacon's H.264 RTP access-unit boundary. It groups same-timestamp Annex B NAL bytes and flushes on marker bit, timestamp change, or source end without copying Sunshine, Moonlight, Artemis, Apollo, Vibeshine, or Vibepollo source.

Milestone 109 note: the replacement cleanup in `GameStreamNativeStreamClient.java` and loopback coverage in `GameStreamLoopbackSessionTest.java` are original Java code for Beacon's GameStream session ownership and no-phone RTP validation boundary. The loopback test uses fake RTSP and RTP socket interfaces around Beacon's production route and does not copy Sunshine, Moonlight, Artemis, Apollo, Vibeshine, or Vibepollo source.

Milestone 111 note: `MoonlightNativeSessionDescriptor.cs`, `MoonlightNativeSessionPlan.java`, the private external-backend session store, owning-client API response field, and Android JSON extraction are original Beacon adapter code. Numeric mappings mirror public constants from the pinned `Limelight.h`; no Moonlight connection implementation, UI, or product settings code is copied.
